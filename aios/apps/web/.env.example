NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Server-side only (never sent to the browser): where the Python AI/analytics
# engine listens. Proxied to via the /api/py rewrite in next.config.js, so the
# engine does not need to be publicly reachable or CORS-configured for the
# browser origin.
PYTHON_API_URL=http://localhost:8000

# Server-side only: where the NestJS API listens. Read per request by the
# /api/v1 route handler, so the same built image can be pointed at a different
# API without rebuilding. NEXT_PUBLIC_API_URL is still honoured as a fallback.
API_URL=http://localhost:4000
